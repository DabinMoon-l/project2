# ============================================================
# Firebase Cloud Functions 환경변수
# ============================================================
# 이 파일을 .env로 복사 후 실제 값을 입력하세요.
# .env 파일은 에뮬레이터 로컬 테스트 시 사용됩니다.

# PPTX 퀴즈 생성 Cloud Run 서비스 URL
# Cloud Run 배포 후 URL을 입력하세요
PPTX_CLOUD_RUN_URL=https://pptx-quiz-generator-xxxxx-an.a.run.app

# ============================================================
# Firebase Secrets (defineSecret)
# ============================================================
# 아래 시크릿은 process.env가 아닌 Firebase Secret Manager로 관리됩니다.
# 설정 방법: firebase functions:secrets:set <SECRET_NAME>
#
# GEMINI_API_KEY      - Google Gemini API 키 (AI 문제 생성, OCR 전처리, 교수 출제 분석 등)
# ANTHROPIC_API_KEY   - Anthropic Claude API 키 (서술형 AI 채점용)
# CLOVA_OCR_API_KEY   - Naver CLOVA OCR API 키 (OCR 텍스트 추출)
